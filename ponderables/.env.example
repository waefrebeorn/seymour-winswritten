# Patreon uploader credentials (gitignored — NEVER commit real tokens)
# Get these from https://www.patreon.com/portal/registration/register-clients
# Create a V2 client, request the `campaigns.posts.create` scope.
PATREON_ACCESS_TOKEN=REPLACE_WITH_YOUR_CREATOR_ACCESS_TOKEN
PATREON_CAMPAIGN_ID=REPLACE_WITH_YOUR_CAMPAIGN_ID
